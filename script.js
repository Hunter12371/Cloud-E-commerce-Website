// Sons & Co. Legacy Script / Wrapper if needed
// Canonical product dataset is managed in products.js.
// Keeping lightweight helper functions if referenced.

// App State
let activeSeason = "all";
let activeCategory = "all";
let searchQuery = "";

// Initialize application if script.js is loaded standalone
window.addEventListener("DOMContentLoaded", () => {
    // Shared initializations handled across pages via cart.js and products.js
});
